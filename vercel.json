{
  "version": 2,
  "builds": [
    { "src": "frontend/**", "use": "@vercel/node", "installCommand": "npm install --force"},
    { "src": "backend/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/$1" },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
