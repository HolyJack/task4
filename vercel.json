{
  "version": 2,
  "builds": [
    { "src": "frontend/**", "use": "@vercel/node", "env": { "NPM_FLAGS": "--force" } },
    { "src": "backend/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/$1" },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
